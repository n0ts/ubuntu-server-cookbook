#!/bin/sh

# Generated by Chef for <%= node['hostname'] %>.
# Local modifications will be overwritten.

PATH="/usr/local/bin:/usr/local/sbin:/usr/bin:/usr/sbin:/bin:/sbin"


case "$IFACE" in
  <%= @params[:name] %>)
    <%- @params[:routes].each do |v| %>
    route <%= @route_mode %> <%= v %> dev <%= @params[:name] %>
    <%- end %>
    ;;
esac
